package model;

public enum StatusReserva {
    PENDENTE,     // A reserva foi criada, mas ainda não foi confirmada
    // CONFIRMADA,   // A reserva foi confirmada
    // CANCELADA,    // A reserva foi cancelada
    CONCLUIDA; 
}
